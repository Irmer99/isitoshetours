services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/templates/default.conf.template:ro
      - ./uploads:/app/uploads:ro
      - certbot-conf:/etc/letsencrypt:ro
      - certbot-www:/var/www/certbot
    depends_on:
      - frontend
      - backend
    restart: unless-stopped

  frontend:
    image: ghcr.io/irmer99/isitoshetours-frontend:latest
    environment:
      - PORT=5173
      - VITE_API_URL=/api
      - NODE_ENV=production
    restart: unless-stopped

  backend:
    image: ghcr.io/irmer99/isitoshetours-backend:latest
    env_file:
      - ./ishe_backend/.env
    environment:
      - PORT=3000
      - CORS_ORIGIN=http://localhost,https://localhost
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped

  certbot:
    image: certbot/certbot
    volumes:
      - certbot-conf:/etc/letsencrypt
      - certbot-www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    profiles:
      - ssl

volumes:
  certbot-conf:
  certbot-www:
